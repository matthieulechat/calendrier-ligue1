---
id: BLK-008
type: blocker
date: 2026-07-19
tags:
  [pwa, service-worker, vite-plugin-pwa, dev-mode, preview, beforeinstallprompt]
---

# BLK-008 — Bouton install PWA invisible en `pnpm dev` (SW non enregistré)

| Friction                                                                                                                                                                                                                                          | Cause réelle                                                                                                                                                                                                                                                                                                                      | Solution                                                                                                                                                                                                                                                                                                               | Statut |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Baptiste signale n'avoir aucun élément lié à la PWA visible (pas de bouton, pas de prompt d'installation) après l'ajout de l'`InstallFab`, alors que le code avait été validé par simulation d'event `beforeinstallprompt` en session précédente. | `vite-plugin-pwa` ne génère aucun service worker en mode `pnpm dev` (comportement documenté, voir aussi GLRN-137 en mémoire globale) — sans SW actif, Chrome ne peut jamais déclencher `beforeinstallprompt`, donc `canInstall` reste `false` et le FAB reste masqué à raison. Baptiste testait très probablement via `pnpm dev`. | Vérifié en conditions réelles via `pnpm build && pnpm preview` : le SW s'active (`"activated"` confirmé via `navigator.serviceWorker.getRegistrations()`), et le FAB apparaît spontanément sans event simulé. Consigne donnée à Baptiste : toujours valider le comportement PWA via `pnpm preview`, jamais `pnpm dev`. | résolu |

## Références

- [BDR-018](../decisions/BDR-018.md) — décision d'implémentation de l'InstallFab à l'origine de ce signalement
