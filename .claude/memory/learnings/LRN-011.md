---
id: LRN-011
type: learning
date: 2026-07-03
tags: [shadcn, base-ui, select, select-value, gotcha, base-nova, radix]
---

# LRN-011 — `@base-ui/react` Select.Value : valeur brute, pas le texte de l'item

| Pattern observé                                                                                                                                                      | Contexte                                               | Application future                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| `Select.Value` de base-ui affiche la valeur raw (ex: `"lemans"`) et non le texte de l'item sélectionné, même quand `ItemText` est présent — contrairement à Radix UI | `Select` shadcn style base-nova dans calendrier-ligue1 | Toujours passer le label explicitement en children de `SelectValue` : `<SelectValue>{selectedItem.name}</SelectValue>` |
